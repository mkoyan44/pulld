apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "pulld.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pulld.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  selector:
    matchLabels:
      {{- include "pulld.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "pulld.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "pulld.serviceAccountName" . }}
      automountServiceAccountToken: {{ .Values.serviceAccount.automountServiceAccountToken }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.priorityClassName }}
      priorityClassName: {{ .Values.priorityClassName | quote }}
      {{- end }}
      {{- if .Values.runtimeClassName }}
      runtimeClassName: {{ .Values.runtimeClassName | quote }}
      {{- end }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: pulld
          image: {{ include "pulld.image" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            {{- if eq .Values.cache.backend "s3" }}
            - {{ .Values.s3.scratchDir | quote }}
            {{- else }}
            - {{ .Values.cache.dir | quote }}
            {{- end }}
            {{- range .Values.extraArgs }}
            - {{ . | quote }}
            {{- end }}
          ports:
            - name: http
              containerPort: 5050
              protocol: TCP
          env:
            - name: PULLD_CACHE_BACKEND
              value: {{ .Values.cache.backend | quote }}
            {{- if eq .Values.cache.backend "s3" }}
            - name: PULLD_S3_ENDPOINT
              value: {{ required "s3.endpoint is required when cache.backend=s3" .Values.s3.endpoint | quote }}
            - name: PULLD_S3_BUCKET
              value: {{ .Values.s3.bucket | quote }}
            - name: PULLD_S3_REGION
              value: {{ .Values.s3.region | quote }}
            - name: PULLD_S3_FORCE_PATH_STYLE
              value: {{ .Values.s3.forcePathStyle | quote }}
            - name: PULLD_S3_SCRATCH_DIR
              value: {{ .Values.s3.scratchDir | quote }}
            {{- if .Values.s3.prefix }}
            - name: PULLD_S3_PREFIX
              value: {{ .Values.s3.prefix | quote }}
            {{- end }}
            - name: PULLD_S3_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: {{ required "s3.existingSecret is required when cache.backend=s3" .Values.s3.existingSecret | quote }}
                  key: {{ .Values.s3.accessKeyIdKey | quote }}
            - name: PULLD_S3_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ required "s3.existingSecret is required when cache.backend=s3" .Values.s3.existingSecret | quote }}
                  key: {{ .Values.s3.secretAccessKeyKey | quote }}
            {{- end }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- if .Values.probes.liveness.enabled }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.liveness.failureThreshold }}
          {{- end }}
          {{- if .Values.probes.readiness.enabled }}
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.readiness.failureThreshold }}
          {{- end }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            {{- if ne .Values.cache.backend "s3" }}
            - name: cache
              mountPath: {{ .Values.cache.dir }}
            {{- end }}
            - name: tmp
              mountPath: /tmp
            {{- with .Values.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        {{- if ne .Values.cache.backend "s3" }}
        - name: cache
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "pulld.cacheClaimName" . }}
          {{- else }}
          emptyDir: {}
          {{- end }}
        {{- end }}
        - name: tmp
          emptyDir: {}
        {{- with .Values.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
